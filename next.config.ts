import type { NextConfig } from "next";

const nextConfig: NextConfig = {
console.log("Attempting to require @tailwindcss/postcss:", require('@tailwindcss/postcss'));

  /* config options here */
};

export default nextConfig;
